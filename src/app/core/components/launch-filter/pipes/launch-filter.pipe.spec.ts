import { LaunchFilterPipe } from './launch-filter.pipe';

describe('LaunchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LaunchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
